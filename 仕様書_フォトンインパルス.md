# メギドWordle：フォトンインパルスシステム 仕様書

## 1. システム概要
メギド72の戦術「フォトンインパルス」をモチーフとした、文字数の少ないメギドを入力した際にプレイヤーがメリット（ヒント）を得られる救済・戦略システム。

## 2. インパルスエネルギー（蓄積）
- **蓄積条件**: プレイヤーがメギド名を入力・確定した際に蓄積。
- **計算式**: `エネルギー増加量 = 8 - 入力メギドの文字数`
  - 文字数が少ないほど、より多くのエネルギーが溜まる。
- **上限（キャップ）**: 35

## 3. エネルギー解放（リリース）
- **使用回数**: 1ゲーム（デイリー/フリー）につき **1回のみ** 使用可能。
- **必要エネルギー**: 25以上。
- **効果（ヒント解放）**:
  - **25 ～ 29**: ヒントを1つランダムに表示。
  - **30 ～ 34**: ヒントを2つランダムに表示。
  - **35 (最大)**: ヒントを3つランダムに表示。

## 4. ヒントの内容
ヒントは `hints.js`（後述のデータ連携により生成）から、正解のメギドに対応する情報が選出される。
- スタイル（例：スタイル：バースト）
- クラス（例：クラス：トルーパー）
- 身長・性別（例：身長164cm 女性）
- 飛行特性（地形無効の有無）
- 攻撃回数（2回以上の場合のみ「アタックが複数回攻撃」と表示）
- 年齢・誕生日（例：転生日/誕生日：6月29日 26歳）
- 内部属性（例：内部属性：水）

※データが「-」などの空欄扱いの場合、その項目は抽選対象から除外される。
※回答はリジェネ前後を区別しないが、ヒントはリジェネ後のものが表示される場合がある。

## 5. UI/UX デザイン
### リリースボタンの状態変化
ボタンの色とラベルがエネルギー量に応じて変化し、視覚的に状態を伝える。
- **0 ～ 24**: `btn-disabled` (灰色) / カーソル：`help` (?)
  - クリックすると説明モーダルが表示される。
- **25 ～ 29**: `btn-ready-1` (薄ピンク) / ラベル：現在のエネルギー数
- **30 ～ 34**: `btn-ready-2` (紫) / ラベル：現在のエネルギー数
- **35 (最大)**: `btn-ready-3` (濃い赤紫) / ラベル：「最大」
- **使用後**: `btn-exhausted` (灰色) / ラベル：「消耗」
  - クリックすると「リリースは1回しか使用できません」と警告が表示される。

### ヒント表示
- 盤面（入力エリア）の上部に、カード形式で縦にスタックされて表示される。
- モバイルユーザー向けに余白とサイズを最適化し、盤面を圧迫しない設計。

## 6. データ連携フロー
1. **Excel管理**: `メギドリスト.xlsx` の `master` シートでデータを管理。
2. **CSV出力**: `メギドリスト.csv` (Shift-JIS/CP932) として保存。
3. **変換実行**: `python excel_to_hints.py` を実行。
   - CSVから必要な列（スタイル、クラス、身長、性別、飛行、攻撃回数、年齢、誕生日/転生日、内部属性）を抽出。
   - 空欄や無効なデータをクレンジング。
4. **反映**: `hints.js` が生成され、Webアプリ側で即座に利用可能になる。

## 7. その他関連仕様
- **チェイン表示**:
  - デイリーモードおよびフリーモードで継続数をカウント。
  - 結果画面のトロフィー絵文字が、特定の数（39, 59, 72）のときのみ特別なアイコンに変化する。
- **リザルトUI**:
  - ゲーム終了（クリア/失敗）時には、リリースボタンなどのプレイ中用UIを非表示にし、結果に集中できるように制御。
